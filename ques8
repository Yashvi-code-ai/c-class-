#include <stdio.h>

int main() {
    int num;

    
    printf("Enter an integer: ");
    scanf("%d", &num);

    
   (num & 1) ? printf("The number is odd\n") : printf("The number is even\n");

    return 0;
}
